<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Malusi's MT5 WebTerminal Bridge</title>
    <style>
      html, body {
        height: 100%;
        width: 100%;
        margin: 0;
        background: #0f111a;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial;
      }
      .wrap {
        display: flex;
        flex-direction: column;
        height: 100%;
        width: 100%;
      }
      .header {
        padding: 12px 16px;
        background: #151821;
        color: #72e3a6;
        font-weight: 600;
        font-size: 16px;
        border-bottom: 1px solid #202431;
      }
      .frame {
        flex: 1;
        overflow: hidden;
      }
      iframe {
        width: 100%;
        height: 100%;
        border: none;
      }
      .footer {
        padding: 10px 16px;
        background: #0f111a;
        color: #bfbfbf;
        font-size: 13px;
        border-top: 1px solid #202431;
      }
      .btn {
        color: #72e3a6;
        text-decoration: underline;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <div class="wrap">
      <div class="header">Malusi's MT5 WebTerminal Bridge</div>
      <div class="frame">
        <iframe 
          src="https://webterminal.metatrader5.com/" 
          title="MT5 WebTerminal" 
          allow="clipboard-read; clipboard-write">
        </iframe>
      </div>
      <div class="footer">
        If the terminal doesn’t load, click 
        <a class="btn" href="https://webterminal.metatrader5.com/" target="_blank" rel="noopener">Open in new tab</a>.
      </div>
    </div>
  </body>
</html>
